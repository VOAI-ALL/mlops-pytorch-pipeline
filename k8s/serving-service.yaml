apiVersion: v1
kind: Service
metadata:
  name: model-serving
  namespace: ml-training
spec:
  selector:
    app: model-serving
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP